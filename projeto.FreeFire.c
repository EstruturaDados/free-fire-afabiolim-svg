#include <stdio.h>
#include <string.h>


#define MAX_ITENS 10

// Estrutura da mochila
typedef struct {
    char nome[30];
    char tipo[20];
    int quantidade;
} Item;

// Aricipal
int main(){
    Item mochila[MAX_ITENS];
    int total = 0;
    int opcao;

    do
    {
        /* code */
    } while (condition);
    
}